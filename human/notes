The bitvec sentinel for length trick is good
The reverse map is interesting.
subst. 0*x = 0  = 0*x*x*x*x*x ... so we need to remember visited.
parser
why is find_mut recursive?
should be checking we don't try to make more vars than 7
add extract

How is lambda pattern being returned?


lambda x y z, ?r * ?q * ?p * 0  should have 3^3 results


lambda x, ?p * 0 =? lambda x, 0
what should the answer be? Hmm. maybe it should be var10. Ok. That's simple

Try binary application. Faster or slower?

(map f (map g x) = map (comp f g) x
comp f g = lam x   (subst g.body x)
comp (lam x N) (lam y M) = lam z (subst N (x (subst M (y z))

Use slotted examples

concrete meet and join examples


So patterns needed some conditions around lambda?
multi lambda binders and multisubstitution
#subst #+    built-ins with special sigil

Better sigil? Does racket / sceheme have a convention?


proof objects. Write only log.
But then process them into what?
We don't have to have purely first order proof objects now.
subst(lam x pos)
trans()


lambda egg
legg
lambda egglog?





```sh
rustup target add wasm32-unknown-unknown
cargo install wasm-bindgen-cli --version 0.2.128 --locked
./build-web.sh
python3 -m http.server
```

bugsum10

(lam x (lam y (lam z ... (a x y z ...))
(lam x (lam y (?m x y)) = (lam x (lam y (?m y x))
sum (lam x (sum (lam y ( ...


(@sum i (@sum j (@sum k (* (a i j) (a j k)))

(rewrite (sum i ?a) (* ?a (sum i 1))

In substitute, why memoize by fatid. Shouldn't we memoize by rawid?


multiterm
ephemeral enodes
python bindings?

explore binary application?
Hmm. but miller patterns are kind of not binary. Well there's implicit currying usually.

Subst doesn't need to contain the symbol
And I think we could cache the permutation the permutation needed in the rewrite somewhere and avoid mlam

Why should be bother linking_root in rev? Is there an instance where we aren't rebuilding it anyway? 

substitute should do simulateous substitution?

Term and Pattern are kind of duplicated no?

Subst being in an arena would probably help. Don't return vec![], insert into arena
Predetermining the variable order would save space. and determine how much to bump allocate

((lam x (?a)) f)


Is it worth having both substtitute and substitute_many?


Do the query string based sharing thing?

Typechecking example
multipatterns
AC10 example
integration
differentiation
Sadly I'd grown to kind of like my d(x) exteriori derivative versoin.


(extract t :depth :size :bindernum (:weight foo 2) (:weight frog 7))  extract by lex ordering of depth, size, bindernum, # of foo occurence
(weight foo 2)  ; give foo weight 2

Try some dag extraction techniques from extraction gym?
ASP or linear alg extract?

I guess I could use egglog parser as dependency and add egglog parsing mode?

TPTP problems + logic rules

Compare with zipperposition? duper? Just like lean automation?
Mechanical mathematicians https://dl.acm.org/doi/pdf/10.1145/3557998

(true
(@forall x ?a) -> ?a ; don't use x
(@exists (and ?a ?b) = (and (exusts ) (exists))
(exists (or a b)) = (exists a) 

forall x exists y body x y ->  exists y (forall x  (?body x (y x)))


Why have Atom and App? It's a reasonable optimization I guess. And it's one step towards


; map f (map g xs) = map (f . g) xs
; Writing composition as a lambda exercises capture-avoiding substitution.
;(rewrite
;  (map ?f (map ?g ?xs))
;  (map (lam z (app ?f (app ?g z))) ?xs))

; but app isn't anything special?

(rewrite 
  (map (lam x (?body x)) (map (lam y (?f y)) ?xs))
  (map (lam z (?body (?f z))))
)

; why do I have to keep
(rewrite (comp ?f ?g) (lam z (f )))


(rewrite (comp ?f ?g) (lam x (?f (?g ?x))))
(rewrite ((comp ?f ?g) x) (?f (?g ?x))
(rewrite (lam (#bind x ))
(rewrite ((@lam x (?body x) e) (?body e))


Is multibiders important?
Should binder auto collapse into multibinders? How would I implement it? Nah.
enum Bind(Symbol, u8, Id)

Bind

App(Id, vec![]) rather than
Or steal a bit to distinguish between Id and Symbol?

subst enough to kill free variables (a x y k l) and the rest go in an app.
Have both binary app and
HOApp(Id, Vec![])
(f a b)
((f) a b)

(?a i j) is ambigous if it's a miller pattern or otherwise... Hmm.
((?a) a b) for ho

(?a @i @j)  ?

[?a i j]
vs
(?a ?x)
(f (a) (b)) Is this accepted?

[a i j]

[a i j] as macro for curried (app a i j)


proof storage.
enum {


}

(@sum x body) and [@sum x body] should be the same thing?
Or another kind of bracket? <sum x body>

Maybe the haskell one hsould just be entirely using []


I'd inline instatiiate_bindings
and I'd panic rather than Option out for stuff that really shouldn't happen.